# Gradle JVM 配置
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8

# Android X 迁移
android.useAndroidX=true

# 并行构建（CI 环境中禁用，避免内存竞争）
org.gradle.parallel=false

# 构建缓存
org.gradle.caching=false

# Kotlin 增量编译
kotlin.incremental=false
